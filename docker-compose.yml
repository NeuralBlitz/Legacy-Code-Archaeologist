version: '3.8'
services:
  archeologist:
    build: .
    env_file: .env
    volumes:
      - ./reports:/app/reports
      - /path/to/target/project:/codebase
    command: audit /codebase --output reports/final.html
